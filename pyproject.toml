[tool.poetry]
name = "bitter-edgar"
version = "0.1.0"
description = "Scale beats cleverness. SEC filings MCP that saves to disk."
authors = ["bxxd <bxxd@users.noreply.github.com>"]
readme = "README.md"
packages = [{include = "bitter_edgar"}]

[tool.poetry.dependencies]
python = "^3.10"
mcp = "^1.0.0"
edgartools = "^4.0.0"
markdownify = "^0.13.0"

[tool.poetry.scripts]
bitter-edgar = "bitter_edgar.server:main"

[tool.poetry.group.dev.dependencies]
watchdog = "^6.0.0"
pytest = "^8.4.2"
pytest-asyncio = "^1.2.0"
pytest-mock = "^3.15.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
